public class object_oriented_programing {
    public static void main(String[] args){

        //Object =An entity that holds data .(attributes)
        //        and can perform actions (methods)
        //        It is a reference datatype

        Car car = new Car();

        /*
        System.out.println(car);
         */ //shows memory address

        /*
        car.isAvailable = true;
        */ // to change the attributes

        /*
        System.out.println(car.make);
        System.out.println(car.name);
        System.out.println(car.color);
        System.out.println(car.price);
        System.out.println(car.year);
        System.out.println(car.isAvailable);
        */ //to access the attributes

        /*
        car.start();
        car.stop();
        */ //to access the methods.

        // how many car objects you create they all will have same properties.
    }
}
